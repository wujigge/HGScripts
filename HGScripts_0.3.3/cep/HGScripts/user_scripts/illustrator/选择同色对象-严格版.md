# 选择同色对象-严格版

## 用途

以第一个选择对象为样本，选中所有填充和描边状态相同、颜色相同的对象。

## 怎么用

1. 选中一个取样对象。
2. 运行 `选择同色对象-严格版.jsx`。

## 匹配规则

- 样本有填充和描边：目标也必须同时有相同填充色和相同描边色。
- 样本只有填充：目标也必须只有相同填充色，不能有描边。
- 样本只有描边：目标也必须只有相同描边色，不能有填充。
- 样本无填充无描边：选择所有无填充无描边空对象。

## 注意

- 组和复合路径会递归取样和匹配。
- 脚本只改变选择，不会改色。
