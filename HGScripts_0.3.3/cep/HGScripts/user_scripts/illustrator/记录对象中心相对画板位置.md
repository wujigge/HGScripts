# 记录对象中心相对画板位置

## 用途

记录选中对象中心点在其所在画板中的相对位置。

配合 `应用对象中心相对画板位置.jsx` 使用。

## 怎么用

1. 选中参考对象。
2. 运行 `记录对象中心相对画板位置.jsx`。
3. 再选中目标对象。
4. 运行 `应用对象中心相对画板位置.jsx`。

## 注意事项

- 记录的是几何中心。
- 多选时记录整体选择范围的中心。
- 记录后可以多次应用到不同对象。
