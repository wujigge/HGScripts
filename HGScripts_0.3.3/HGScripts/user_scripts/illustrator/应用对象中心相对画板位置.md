# 应用对象中心相对画板位置

## 用途

把当前选中对象移动到已记录的画板相对位置。

配合 `记录对象中心相对画板位置.jsx` 使用。

## 怎么用

1. 先运行 `记录对象中心相对画板位置.jsx` 记录参考位置。
2. 选中要移动的目标对象。
3. 运行 `应用对象中心相对画板位置.jsx`。

## 结果

- 目标对象会移动到自己所在画板中的对应位置。
- 多选时会整体移动，保持对象之间的相对位置。

## 注意事项

- 对齐的是几何中心。
- 不需要依赖选择顺序或图层顺序。
- 记录位置后可以重复应用。
