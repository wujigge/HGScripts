# 选择同色对象-宽松版

## 用途

以第一个选择对象为样本，按更宽松的单色规则选择同色对象。

## 怎么用

1. 选中一个取样对象。
2. 运行 `选择同色对象-宽松版.jsx`。

## 匹配规则

- 样本有填充和描边且颜色不同：目标也必须同时有相同填充色和相同描边色。
- 样本只有一种颜色：目标只要所有存在的填充/描边都是同一颜色即可，不强制填充对填充、描边对描边。
- 样本无填充无描边：选择所有无填充无描边空对象。

## 注意

- 组和复合路径会递归取样和匹配。
- 脚本只改变选择，不会改色。
