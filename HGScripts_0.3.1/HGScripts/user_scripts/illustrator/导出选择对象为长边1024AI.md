# 导出选择对象为长边1024AI

## 用途

把当前选中对象导出成新的 Illustrator 文件，并把对象长边标准化为 1024。

适合制作统一尺寸的素材文件。

## 怎么用

1. 打开 Illustrator 文档。
2. 选中要导出的对象。
3. 运行 `导出选择对象为长边1024AI.jsx`。
4. 按提示选择保存位置。

## 结果

- 生成一个新的 AI 文件。
- 选中对象会缩放到长边 1024。
- 新文件画板会适配缩放后的对象。

## 注意事项

- 会创建新文件，不会直接覆盖原文档。
- 导出前建议确认只选中了需要导出的对象。
